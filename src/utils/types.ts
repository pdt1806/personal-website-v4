export interface TechnologiesType {
  name: string;
  icon?: string;
}

export interface WorkType {
  title: string;
  description: string;
  img: string;
  source: string;
  url: string;
  collaborators?: string[];
}
